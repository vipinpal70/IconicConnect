import { MillingLayout } from "@/src/components/MillingLayout";

export default function MillingDashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return <MillingLayout>{children}</MillingLayout>;
}
