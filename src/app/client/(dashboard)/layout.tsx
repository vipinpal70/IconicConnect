import { ClientLayout } from "@/src/components/ClientLayout";

export default function ClientDashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return <ClientLayout>{children}</ClientLayout>;
}
